#ifndef TAPE_H
#define TAPE_H

// Объявление
//extern unsigned char tape[]; Делает массив видимым в других файлах

// Занять память
char AllocateMemory();

// Освободить память
char FreeMemory();

#endif