mov rax, 1
mov rcx, 104
mov rcx, 14       ; Передаем адрес ячейки данных (теперь это 14 из-за пробелов-строк)
mov rsi, rcx
mov rcx, 104
mov [rsi], rcx
mov rcx, [rsi]
syscall
hlt

0                 ; Сюда процессор запишет данные динамически