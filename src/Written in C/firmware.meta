main_loop:
 INT             # Печатает букву, Си сам сдвигает gpl_ip на букву
 INC GPL_IP      # Сдвигаем gpl_ip на следующий маркер
 JE exit_system  # Если мы наступили на 0 -> прыгаем на HLT
 JMP main_loop   # ИНАЧЕ просто безусловно прыгаем в начало цикла!

exit_system:
 HLT