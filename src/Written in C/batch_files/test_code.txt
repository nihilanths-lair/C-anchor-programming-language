static unsigned char memory[0xFFFF+0x01] = // Для быстрого теста/проверки работоспобности движка
{
    /*
    ; ---- Данные строки прямо в коде ----
    ; (условно, после инструкций размещаем байты)
    JMP copy_start
    string_data: db "Hello"
    copy_start:
    ; загружаем адрес источника (string_data) в указатель p8
    MOV p8, i8,  string_data   ; p8 = адрес строки (предположим, метка вычислена ассемблером)
    ; загружаем адрес назначения в регистр r8 или в m8
    MOV r8, i8,  0x10          ; r8 = целевой адрес
    ; копируем 5 байт
    MOV m8, p8,  r8            ; memory[r8] = memory[memory[p8]] – читаем байт из source по указателю и пишем по адресу из r8
    INC p8                     ; увеличиваем указатель источника
    INC r8                     ; увеличиваем указатель назначения
    ... повтор 5 раз
    */
    jmp8_si, 7,              // 0: jmp 7
    'H', 'e', 'l', 'l', 'o', // 2: Hello
    9, 2,                    // 7: mov r8 << i8 ; mov i8 >> r8
    hlt                      // 9: hlt
};